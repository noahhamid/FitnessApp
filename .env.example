# Server (Hono + Prisma)
DATABASE_URL=
DIRECT_URL=
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=
PORT=3000

# Expo client — use your LAN IP so physical devices can reach the API
EXPO_PUBLIC_API_URL=
EXPO_PUBLIC_BETTER_AUTH_URL=
