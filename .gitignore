# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
.expo-web-test/
.expo-web-test2/
.expo-web-test*/
dist/
dist-web/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# This project uses npm (package-lock.json). Ignore pnpm artifacts so Vercel
# does not auto-switch to a stale pnpm-lock.yaml.
pnpm-lock.yaml
pnpm-workspace.yaml
.pnpm-store/

# macOS
.DS_Store
*.pem

# local env files (.env.example is the committed checklist)
.env*
!.env.example

# Upstream exercises-dataset dump (17MB+) — regenerate curated seed from it locally
/data/exercises-dataset.json

# User-uploaded meal scan photos (served from /uploads)
/uploads/

# typescript
*.tsbuildinfo

# Prisma generated client — regenerated by `postinstall` / `prisma generate`
/prisma/generated/

app-example

# generated native folders
/ios
/android

.vercel
api/handler.cjs
